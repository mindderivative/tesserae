# MD3's linear progress indicator (Tesserae's `controls.LinearProgress`,
# M40): `value` in 0..1, `width`/`height` its track.
params: [width, height, value]
id: root
kind: LinearProgress
value: "{{ value }}"
style:
  width: "{{ width }}"
  height: "{{ height }}"
