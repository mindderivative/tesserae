# MD3's time picker dial (Tesserae's `controls.TimePickerDial`, M40):
# `hour` (0..23) and `minute` (0..59) set the hands, `size` the face.
params: [size, hour, minute]
id: root
kind: TimePickerDial
hour: "{{ hour }}"
minute: "{{ minute }}"
style:
  width: "{{ size }}"
  height: "{{ size }}"
