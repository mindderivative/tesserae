# MD3's radio button (Tesserae's `controls.RadioButton`, M40): `selected`
# is its state and `size` its touch target. A click selects it.
params: [size, selected]
id: root
kind: RadioButton
selected: "{{ selected }}"
style:
  width: "{{ size }}"
  height: "{{ size }}"
