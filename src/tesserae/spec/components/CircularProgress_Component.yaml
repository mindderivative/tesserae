# MD3's circular progress indicator (Tesserae's
# `controls.CircularProgress`, M40): `value` in 0..1, `size` its box.
params: [size, value]
id: root
kind: CircularProgress
value: "{{ value }}"
style:
  width: "{{ size }}"
  height: "{{ size }}"
